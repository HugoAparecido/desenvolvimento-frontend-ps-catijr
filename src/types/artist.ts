export interface RecentArtist {
    id: string,
    name: string,
    about: string,
    createdAt: string,
    updatedAt: string,
}

export interface ArtistActionInfo {
    id: string | number,
    isfixed: boolean
}