export interface RecentArtist {
    id: string,
    name: string,
    about: string,
    createdAt: string,
    updatedAt: string,
}

export interface MostPlayedArtists {
    id: string,
    name: string,
    listeners: number,
    about: string,
    createdAt: string,
    updatedAt: string,
}